@using Confidami.Common
@model Confidami.Model.Menu
<!-- navbar -->
<nav class="navbar navbar-default">

    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>

        @Html.ActionLink("Homepage", "Index", "Home", new object { }, new { @class = "navbar-brand" })

    </div>
    <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav">

            <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="themes">Segnalazioni<span class="caret"></span></a>
                <ul class="dropdown-menu" aria-labelledby="themes">
                    <li>@Html.ActionLink("Tutte", ActionsStore.Contents, ControllerStore.Contents)</li>
                    <li>@Html.ActionLink("Inserisci", ActionsStore.ContentsInsert, ControllerStore.Contents)</li>
                    <li>@Html.ActionLink("Cerca", "", ControllerStore.Search)</li>
                    <li>@Html.ActionLink("Modifica", ActionsStore.FindEditCode, ControllerStore.Contents)</li>
                </ul>
            </li>

            <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="themes">Categorie<span class="caret"></span></a>
                <ul class="dropdown-menu" aria-labelledby="themes">
                    @foreach (var cat in Model.Categories)
                    {
                        <li>@Html.RouteLink(cat.Description, RouteStore.Category,new { id=cat.IdCategory, slug=cat.Slug },null)</li>
                    }
                </ul>
            </li>

            <li>@Html.ActionLink("Chi siamo", ActionsStore.About, ControllerStore.Home)</li>
            <li>@Html.ActionLink("Contatti", ActionsStore.Contact, ControllerStore.Home)</li>
            <li>@Html.ActionLink("Newsletter", ActionsStore.Newsletter, ControllerStore.Newsletter)</li>
         
        </ul>

        <!-- search box -->
        <form class="navbar-form navbar-right" role="search" action="/cerca" method="post" >
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Cerca" name="k">
            </div>
            <button type="submit" class="btn btn-primary btn-sm">Cerca</button>
        </form>                
        @*@Html.Partial("_LoginPartial")*@
    </div>

</nav>
<!-- end navbar -->