@using Confidami.Common
@model Confidami.Model.Menu
<!-- navbar -->
<nav class="navbar navbar-default">
    
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>

        @Html.ActionLink("Homepage", "Index", "Home", new object { }, new { @class = "navbar-brand" })

    </div>
    <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
            
            <!-- da spostare solo in area admin dopo login -->
            @if (User.IsInRole(RolesStore.AdminRole))
            {
                <li>@Html.ActionLink(@ViewsStore.Moderation, ActionsStore.Moderation, ControllerStore.Manager)</li>
            }  


            

            <!-- da spostare solo in area admin dopo login -->
            
        </ul>

        <!-- search box -->            
        @Html.Partial("_LoginPartial")
    </div>

</nav>
<!-- end navbar -->